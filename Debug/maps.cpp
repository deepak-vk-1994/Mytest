#include<iostream>
#include<fstream>
//#include<vector>
//#include<string>
#include<map>
#include<utility>
#include<tuple>
using namespace std;

int main() {

//	std::pair<double,double> p;
//	std::map<std::pair<double,double>,int> mymap;
//	std::tuple<double,double,double> point;
//	std::map<std::tuple<double,double,double>,int> mymaptuple;

//	for (int i = 0 ; i < 10; i++) {
////		p.first = i*0.245;
////		p.second = 2.2099*i;
////		mymap[p] = i;
//		mymaptuple[std::tuple<double,double,double>(i*0.234,(i+1)*0.457,(i+2)*0.765)] = i;
//	}
////	p.first = 1*0.245;
////	p.second = 1*2.2099;
////	
//	int i =1;
//	point = std::make_tuple(i*0.234,(i+1)*0.45700000,(i+2)*0.765);

//	if(mymaptuple.count(point) > 0) {
//		cout << "Yes"<< endl;
//	}
//	
////	for(std::map<std::tuple<double,double,double>,int>::iterator ii = mymaptuple.begin(); ii != mymaptuple.end(); ++ii) {
////		//cout << (*ii).first.first<<","<<(*ii).first.second<<":"<<(*ii).second << endl;
////		cout<<std::get<0>((*ii).first)<<","<<std::get<1>((*ii).first)<<","<<std::get<2>((*ii).first)<<":"<<(*ii).second<<endl;
////	}

	cout<<sizeof(double);
}
